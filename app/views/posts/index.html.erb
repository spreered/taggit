<h1>Index</h1>
<ul>
  <% @posts.each do  |post|%>
  <li><%=link_to post.title, post_path(post) %> | <%=link_to  "edit", edit_post_path(post) %> | <%= link_to  "delete", post_path(post), method: :delete %></li>
  <%end%>
</ul>
<%= link_to  "new post", new_post_path %>